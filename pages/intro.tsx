import { FC } from "react";

const Intro: FC = (): JSX.Element => {
  return (
    <div>
      <div className="fixed h-screen w-screen z-10">Intro Page</div>
    </div>
  );
};

export default Intro;
